import {
  createModal as _createModal,
  closeLastModal as _closeLastModal,
  onConfirmationPopup as _onConfirmationPopup,
} from "./utils"

export const closeLastModal = _closeLastModal
export const createModal = _createModal
export const onConfirmationPopup = _onConfirmationPopup
